# Needed only because @nx/devkit@23.1.0-rc.2's peer range ("nx >= 22 <= 24 || ^23.0.0-0") rejects the nx 23.1.0-rc.2 prerelease under strict semver. Remove when the workspace moves to nx/@nx 23.1.x stable.
legacy-peer-deps=true
